{
  "name": "@showcase/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Framework-agnostic systems: event bus, condition evaluation, job tracking, asset store, config service, KPI tracking.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}